---
title: Architecture
---

# Syntra Architecture: Deep Dive into AI DevOps Orchestration

> **Published:** 2026-04-03 | **Section:** AI & Automation | **Author:** Amirreza Rezaie

## Overview

**Syntra** is the central AI teammate for the Goalixa platform — an intelligent orchestration service that manages incidents, automates DevOps workflows, and provides seamless Kubernetes cluster management through a multi-agent system powered by CrewAI.

### What Syntra Solves

As a solo developer building Goalixa, I faced three critical challenges:

1. **Incident Management Overload** - Debugging production issues across multiple microservices
2. **Context Switching** - Constantly moving between development, deployment, and operations
3. **Operational Complexity** - Managing Kubernetes clusters, Git repositories, and service health

Syntra addresses these by acting as an **always-available DevOps teammate** that:
- Investigates incidents automatically
- Manages Kubernetes operations with AI
- Provides a unified CLI interface for all operations
- Learns from past incidents to improve future responses

---

## System Architecture

### High-Level Architecture

```mermaid
graph TB
    subgraph "Client Layer"
        CLI[Syntra CLI]
        Web[Admin Panel<br/>syntra.goalixa.com]
    end

    subgraph "API Gateway Layer"
        FastAPI[FastAPI REST API]
        AuthProxy[Auth Proxy]
    end

    subgraph "Orchestration Layer"
        CrewRunner[CrewAI Orchestrator]
        AgentManager[Agent Manager]
    end

    subgraph "Agent Layer"
        Planner[Planner Agent<br/>Intent Recognition & Planning]
        Evidence[Evidence Collector Agent<br/>Diagnostic Data Gathering]
        Incident[Incident Agent<br/>Root Cause Analysis]
        DevOps[DevOps Agent<br/>K8s Operations]
        Reviewer[Reviewer Agent<br/>Change Validation]
    end

    subgraph "Tools Layer"
        K8sTools[Kubernetes Tools<br/>Pods, Logs, Events]
        RuleTools[Rule Tools<br/>Pattern Detection]
        LLMTools[LLM Tools<br/>Claude Integration]
        GitTools[Git Tools<br/>Repo Operations]
    end

    subgraph "Infrastructure Layer"
        K8s[Kubernetes Cluster]
        Git[Git Repositories]
        Memory[(Incident Memory<br/>ChromaDB)]
    end

    subgraph "Authentication & Security"
        GoalixaAuth[Goalixa Auth Service]
        APIKeys[API Key Management]
    end

    CLI -->|API Key / JWT| FastAPI
    Web -->|JWT Session| AuthProxy
    AuthProxy --> FastAPI
    FastAPI --> GoalixaAuth

    FastAPI --> CrewRunner
    CrewRunner --> AgentManager
    AgentManager --> Planner
    AgentManager --> Evidence
    AgentManager --> Incident
    AgentManager --> DevOps
    AgentManager --> Reviewer

    Evidence --> K8sTools
    Evidence --> RuleTools
    Incident --> LLMTools
    DevOps --> K8sTools
    DevOps --> GitTools
    Reviewer --> GitTools

    K8sTools --> K8s
    GitTools --> Git
    Incident --> Memory
    Evidence --> Memory

    style CLI fill:#e1f5fe
    style Web fill:#e1f5fe
    style Planner fill:#fff9c4
    style Evidence fill:#c8e6c9
    style Incident fill:#ffccbc
    style DevOps fill:#b3e5fc
    style Reviewer fill:#d1c4e9
```

---

## Agent System Design

### Agent Hierarchy & Responsibilities

```mermaid
graph LR
    subgraph "Intent Recognition"
        Planner[Planner Agent]
    end

    subgraph "Diagnostic Phase"
        Evidence[Evidence Collector]
    end

    subgraph "Analysis Phase"
        Incident[Incident Agent]
    end

    subgraph "Execution Phase"
        DevOps[DevOps Agent]
    end

    subgraph "Validation Phase"
        Reviewer[Reviewer Agent]
    end

    Planner -->|1. Analyze Request| Evidence
    Evidence -->|2. Collect Data| Incident
    Incident -->|3. Root Cause| DevOps
    DevOps -->|4. Execute Fix| Reviewer
    Reviewer -->|5. Validate| END[Complete]

    style Planner fill:#fff9c4
    style Evidence fill:#c8e6c9
    style Incident fill:#ffccbc
    style DevOps fill:#b3e5fc
    style Reviewer fill:#d1c4e9
```

### Agent Detailed Specifications

#### 1. Planner Agent - The Incident Commander

**Role:** Intent Recognition & Workflow Orchestration

**Capabilities:**
- Natural language request parsing
- Intent classification (diagnose_pod, namespace_overview, collect_evidence, general_help)
- Execution plan generation
- Required information identification
- Agent delegation

**Input Example:**
```
"Investigate why the auth-service pod is crashing in production"
```

**Output:**
```json
{
  "intent": "diagnose_pod",
  "required_params": {
    "namespace": "production",
    "pod_name": "auth-service-*"
  },
  "execution_plan": [
    "EvidenceCollector: Gather pod state, logs, events",
    "IncidentAgent: Analyze patterns and detect root cause",
    "DevOps: Suggest remediation if safe operation detected"
  ]
}
```

---

#### 2. Evidence Collector Agent - The Detective

**Role:** Diagnostic Data Gathering

**Capabilities:**
- Pod state inspection (all containers)
- Log collection with timestamps
- Event filtering and correlation
- Related pod discovery (same deployment, replica sets)
- Kubernetes connectivity verification

**Evidence Package Structure:**
```json
{
  "pod_name": "auth-service-7d4f8b9c-x2k4p",
  "namespace": "production",
  "evidence": {
    "pod_state": {
      "phase": "Running",
      "containers": [
        {
          "name": "auth-service",
          "state": "waiting",
          "reason": "CrashLoopBackOff",
          "restart_count": 5
        }
      ]
    },
    "logs": [
      {
        "container": "auth-service",
        "timestamp": "2026-04-03T10:23:45Z",
        "message": "Connection refused: postgres.goalixa.svc:5432"
      }
    ],
    "events": [
      {
        "type": "Warning",
        "reason": "BackOff",
        "message": "Back-off restarting failed container"
      }
    ],
    "related_pods": [
      "auth-service-7d4f8b9c-y5k7m",
      "auth-service-7d4f8b9c-z9j2n"
    ]
  }
}
```

---

#### 3. Incident Agent - The Analyst

**Role:** Root Cause Analysis & Pattern Detection

**Dual Approach:**

**1. Rule-Based Detection (First Pass)**
- OOM (Out of Memory) kills
- CrashLoopBackOff patterns
- Image pull failures
- Probe failures (readiness/liveness)

**2. LLM Fallback (Unknown Issues)**
- Complex multi-factor failures
- Novel error patterns
- Cross-service dependencies

**Detection Confidence Threshold:** 70%
- Below 70%: Escalate to LLM (Claude)
- Above 70%: Provide rule-based diagnosis

**Output Example:**
```json
{
  "root_cause": {
    "type": "database_connection_failure",
    "confidence": 0.85,
    "detection_method": "rule_based",
    "evidence": [
      "Connection refused error in logs",
      "Database pod not found in namespace",
      "Service endpoint missing"
    ]
  },
  "remediation": {
    "immediate": "Check if PostgreSQL deployment exists in production namespace",
    "verification": "kubectl get pods -n production -l app=postgres",
    "prevention": "Add dependency check in deployment pipeline"
  }
}
```

---

#### 4. DevOps Agent - The Executor

**Role:** Kubernetes Operations Execution

**Capabilities:**
- Pod inspection and status checks
- Log retrieval and filtering
- Service and deployment queries
- Safe operations (read-only by default)
- Namespace overviews

**Safety Model:**
- Default: Read-only access
- Write operations require explicit confirmation
- Destructive operations (delete, scale down) blocked in CLI

---

#### 5. Reviewer Agent - The Validator

**Role:** Change Validation & Quality Assurance

**Capabilities:**
- Execution result validation
- Error detection and reporting
- Best practices compliance checking
- Incident history correlation

---

## Authentication & Authorization

### Dual Authentication System

Syntra implements two authentication mechanisms for different use cases:

```mermaid
sequenceDiagram
    participant CLI as Syntra CLI
    participant API as Syntra API
    participant Auth as Goalixa Auth Service
    participant K8s as Kubernetes
    participant Admin as Admin Panel

    Note over CLI,Auth: CLI Authentication Flow
    CLI->>API: POST /api/ask<br/>X-API-Key: sk_live_xxx
    API->>API: Validate API Key
    API->>API: Check Rate Limits
    API->>Auth: Verify Key Permissions
    Auth-->>API: User + Permissions
    API->>K8s: Execute Operation

    Note over Admin,K8s: Web Authentication Flow
    Admin->>Auth: POST /login
    Auth-->>Admin: Access Token (15min)
    Auth-->>Admin: Refresh Token (7days)

    Admin->>API: GET /admin<br/>Cookie: session_token
    API->>Auth: Validate JWT
    Auth-->>API: User + Roles
    API-->>Admin: Admin Dashboard
```

### API Key Authentication (CLI)

**Endpoint:** `POST /api/ask`

**Headers:**
```
X-API-Key: sk_live_abc123...
Content-Type: application/json
```

**Rate Limits:**
- 60 requests/minute
- 1000 requests/hour
- Per-key limits

**Key Types:**
| Key Prefix | Type | Permissions |
|------------|------|-------------|
| `sk_live_` | Production | Full access |
| `sk_test_` | Test | Read-only |
| `sk_admin_` | Admin | All + key management |

### JWT Authentication (Admin Panel)

**Token Structure:**

**Access Token (15-minute TTL):**
```json
{
  "user_id": "user_abc123",
  "email": "user@example.com",
  "roles": ["admin", "developer"],
  "permissions": [
    "kubernetes:read",
    "kubernetes:write",
    "incident:create"
  ],
  "exp": 1714876800
}
```

**Refresh Token (7-day TTL):**
- Stored in HTTP-only cookie
- Auto-rotates on refresh
- Revoked on logout/password change

---

## Data Flow: Incident Investigation

### Complete Incident Response Flow

```mermaid
sequenceDiagram
    participant U as User
    participant CLI as Syntra CLI
    participant API as FastAPI
    participant P as Planner Agent
    participant E as Evidence Collector
    participant I as Incident Agent
    participant K8s as Kubernetes

    U->>CLI: syntra ask "Why is auth-service crashing?"
    CLI->>API: POST /api/ask<br/>{query: "...", context: "production"}

    API->>P: Analyze intent
    P->>P: Intent: diagnose_pod<br/>Params: {namespace, pod_pattern}
    P-->>API: Execution plan

    API->>E: Collect evidence
    E->>K8s: Get pod state
    E->>K8s: Get logs (all containers)
    E->>K8s: Get events
    E->>K8s: Find related pods

    K8s-->>E: Pod data
    K8s-->>E: Log lines with timestamps
    K8s-->>E: Event list

    E-->>API: Evidence package

    API->>I: Analyze evidence
    I->>I: Check OOM patterns
    I->>I: Check crash loops
    I->>I: Check image failures
    I->>I: Check probe failures

    alt Pattern Match (confidence > 70%)
        I-->>API: Root cause + remediation
    else Unknown Pattern
        I->>I: Call LLM (Claude)
        I-->>API: LLM analysis
    end

    API-->>CLI: Formatted response
    CLI-->>U: Display diagnosis
```

---

## CLI Workflow

### Syntra CLI Interface

```mermaid
graph TB
    Start[User executes command] --> CheckAuth{Authenticated?}

    CheckAuth -->|No| Login[Login with Goalixa Auth]
    Login --> TokenStore[Store JWT securely]
    TokenStore --> CheckAuth

    CheckAuth -->|Yes| ParseCommand[Parse command type]

    ParseCommand --> Direct[Direct Command]
    ParseCommand --> Interactive[Interactive Mode]
    ParseCommand --> Config[Config Management]

    Direct --> Execute[Execute single task]
    Execute --> Display[Display results]

    Interactive --> REPL[Start REPL session]
    REPL --> ReadInput[Read user input]
    ReadInput --> Execute

    Config --> ConfigOps[Set API key, endpoint, etc.]

    Display --> End[Complete]
    REPL --> End
    ConfigOps --> End

    style Start fill:#e1f5fe
    style Direct fill:#c8e6c9
    style Interactive fill:#fff9c4
    style Config fill:#ffccbc
```

### CLI Commands

```bash
# Authentication
syntra login                    # Authenticate with Goalixa Auth
syntra logout                   # Logout and clear credentials
syntra status                   # Show authentication status

# Direct Commands
syntra ask "diagnose pod auth-service"    # Single task execution
syntra health                   # Check Syntra service health
syntra agents                   # List available agents
syntra tools                    # List available tools

# Interactive Mode
syntra                          # Start interactive REPL
syntra repl --enhanced          # Enhanced REPL with history

# Configuration
syntra config set endpoint https://syntra.goalixa.com
syntra config set api-key sk_live_abc123
syntra config list              # Show all configuration
```

---

## Admin Panel

### Admin Panel Features

```mermaid
graph TB
    subgraph "Admin Panel Dashboard"
        Dashboard[Dashboard Overview]

        subgraph "User Management"
            Users[User List]
            UserRoles[Role Assignment]
            UserPerms[Permission Management]
        end

        subgraph "API Key Management"
            Keys[API Keys]
            KeyCreate[Create New Key]
            KeyRevoke[Revoke Key]
            KeyLimits[Rate Limits]
        end

        subgraph "Agent Management"
            Agents[Agent Status]
            AgentConfig[Agent Configuration]
            AgentLogs[Agent Logs]
        end

        subgraph "Incident History"
            Incidents[Incident List]
            IncidentDetail[Incident Details]
            IncidentExport[Export Reports]
        end
    end

    Dashboard --> Users
    Dashboard --> Keys
    Dashboard --> Agents
    Dashboard --> Incidents

    Users --> UserRoles
    Users --> UserPerms

    Keys --> KeyCreate
    Keys --> KeyRevoke
    Keys --> KeyLimits

    Agents --> AgentConfig
    Agents --> AgentLogs

    Incidents --> IncidentDetail
    Incidents --> IncidentExport

    style Dashboard fill:#e1f5fe
    style Users fill:#c8e6c9
    style Keys fill:#fff9c4
    style Agents fill:#ffccbc
    style Incidents fill:#d1c4e9
```

---

## Technology Stack

### Core Technologies

| Component | Technology | Purpose |
|-----------|------------|---------|
| **API Framework** | FastAPI | High-performance REST API |
| **AI Orchestration** | CrewAI | Multi-agent coordination |
| **LLM Integration** | LangChain + Claude | Advanced reasoning |
| **Kubernetes** | Python K8s Client | Cluster operations |
| **CLI** | Typer + Rich | Beautiful command-line interface |
| **Authentication** | Goalixa Auth | Centralized auth service |
| **Vector DB** | ChromaDB (planned) | Incident memory |
| **Python Version** | 3.11+ | Runtime |

---

## Deployment Architecture

### Kubernetes Deployment

```mermaid
graph TB
    subgraph "Goalixa Services"
        CoreAPI[Core API]
        Auth[Auth Service]
        BFF[BFF]
        PWA[PWA]
        Landing[Landing]
    end

    subgraph "Syntra"
        SyntraAPI[Syntra API]
        SyntraAdmin[Admin Panel]
    end

    subgraph "Data"
        Postgres[(PostgreSQL)]
        Memory[(Incident Memory)]
    end

    Ingress[NGINX Ingress] --> CoreAPI
    Ingress --> Auth
    Ingress --> BFF
    Ingress --> PWA
    Ingress --> Landing
    Ingress --> SyntraAPI
    Ingress --> SyntraAdmin

    SyntraAPI --> Auth
    SyntraAPI --> CoreAPI
    SyntraAPI --> Memory
    CoreAPI --> Postgres

    SyntraCLI[Syntra CLI] -.->|HTTPS| SyntraAPI

    style SyntraAPI fill:#b3e5fc
    style SyntraAdmin fill:#b3e5fc
```

---

## Security Model

### Defense in Depth

```mermaid
graph TB
    subgraph "Security Layers"
        L1[Layer 1: Authentication]
        L2[Layer 2: Authorization]
        L3[Layer 3: Rate Limiting]
        L4[Layer 4: Agent Safety]
        L5[Layer 5: Audit Logging]
    end

    L1 --> L2 --> L3 --> L4 --> L5

    L1[JWT / API Key Validation]
    L2[Role-Based Access Control]
    L3[Per-Key Rate Limits]
    L4[Read-Only Default<br/>Confirmation for Writes]
    L5[All Actions Logged]

    style L1 fill:#c8e6c9
    style L2 fill:#fff9c4
    style L3 fill:#ffccbc
    style L4 fill:#b3e5fc
    style L5 fill:#d1c4e9
```

### Security Features

1. **Authentication**
   - JWT tokens with short TTL (15 minutes)
   - API key rotation support
   - HTTP-only cookies for web sessions

2. **Authorization**
   - Role-based access control (RBAC)
   - Granular permissions per operation
   - Namespace-level isolation

3. **Rate Limiting**
   - Per-key limits (60 req/min, 1000 req/hour)
   - IP-based fallback limits
   - Configurable per user role

4. **Agent Safety**
   - Read-only by default
   - Explicit confirmation for destructive ops
   - Operation validation before execution

5. **Audit Trail**
   - All actions logged with user context
   - Incident history tracking
   - Change attribution

---

## Incident Memory System

### Learning from Past Incidents

```mermaid
graph LR
    subgraph "Incident Lifecycle"
        Incident[Incident Occurs]
        Collection[Evidence Collection]
        Analysis[Root Cause Analysis]
        Resolution[Remediation Applied]
        Storage[Store in Memory]
    end

    subgraph "Future Incidents"
        NewIncident[New Incident]
        Retrieval[Retrieve Similar]
        Suggestion[Suggest Solution]
    end

    Incident --> Collection
    Collection --> Analysis
    Analysis --> Resolution
    Resolution --> Storage

    NewIncident --> Retrieval
    Retrieval --> Storage
    Storage --> Suggestion
    Suggestion --> Resolution

    style Storage fill:#d1c4e9
    style Retrieval fill:#fff9c4
    style Suggestion fill:#c8e6c9
```

### Memory Structure (Planned with ChromaDB)

```json
{
  "incident_id": "inc_20260403_001",
  "timestamp": "2026-04-03T10:23:45Z",
  "namespace": "production",
  "affected_resources": ["auth-service-7d4f8b9c-x2k4p"],
  "symptoms": [
    "CrashLoopBackOff",
    "Connection refused to postgres"
  ],
  "root_cause": {
    "type": "database_connection_failure",
    "confidence": 0.85
  },
  "remediation": {
    "action": "deployed missing postgres service",
    "verified": true
  },
  "tags": ["database", "connectivity", "missing-dependency"],
  "embedding": [0.123, 0.456, ...]  // Vector embedding for similarity search
}
```

---

## Current Status & Roadmap

### Implementation Status

**Implemented ✅**
- PlannerAgent with intent recognition
- EvidenceCollectorAgent with K8s integration
- IncidentAgent with rule-based detection
- FastAPI REST API with authentication
- Kubernetes tools (pod state, logs, events)
- CLI framework (basic)
- Admin panel authentication proxy
- API key management with rate limiting

**In Progress 🚧**
- LLM integration (Claude) for complex incidents
- ChromaDB incident memory
- Complete CLI implementation
- Enhanced admin panel

**Planned 📋**
- DevOpsAgent full implementation
- ReviewerAgent full implementation
- Git operations tools
- Multi-cluster support
- Advanced analytics dashboard
- Self-healing automation
- Slack/Discord integration

---

## Key Takeaways

1. **Syntra is a Central AI Teammate** - Not just a tool, but an intelligent assistant that learns and collaborates

2. **Multi-Agent Architecture** - Specialized agents handle different aspects of incident response

3. **Dual Authentication** - Supports both API keys (CLI) and JWT (web) for different use cases

4. **Hybrid AI Approach** - Fast rule-based detection + LLM fallback for complex issues

5. **Safety First** - Multiple security layers, read-only defaults, explicit confirmations

6. **Continuous Learning** - Incident memory system improves future responses

---

## Related Posts

- [Using Claude for Goalixa](../software-engineering/using-claude-for-goalixa.md) - AI development workflow
- [ArgoCD Setup: First Step](../gitops/argocd-first-step.md) - GitOps infrastructure
- [Monitoring Stack Setup](../monitoring-stack-prometheus-grafana-alertmanager.md) - Observability

---

## Connect & Explore

**Syntra Repository:** [https://github.com/goalixa/syntra](https://github.com/goalixa/syntra)

**Goalixa Project:** [https://github.com/goalixa](https://github.com/goalixa)

**Live Demo:** [https://syntra.goalixa.com](https://syntra.goalixa.com)

---

**Tags:** `#ai` `#architecture` `#devops` `#multi-agent` `#kubernetes` `#system-design` `#crewai` `#orchestration`
